class Solution:
    def orderOfLargestPlusSign(self, N, mines):
        """
        :type N: int
        :type mines: List[List[int]]
        :rtype: int
        """
        M = (N+1)//2+1
        for order in reversed(range(M+1)):
            # XXX
            # check

print(Solution().orderOfLargestPlusSign(i, []))
