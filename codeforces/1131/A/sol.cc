#include <bits/stdc++.h>

using namespace std;

int main()
{
    ios::sync_with_stdio(false);
    cin.tie(0);
    long long w1, h1, w2, h2;
    cin >> w1 >> h1 >> w2 >> h2;
    cout << 4ll + 2ll*(h1+h2) + 2ll*w1 << endl;
    return 0;
}
