#include <bits/stdc++.h>

using namespace std;

int main()
{
    ios::sync_with_stdio(false);
    cin.tie(0);
    int t, x, y;
    cin >> t;
    while (t--) {
        cin >> x >> y;
        cout << x << ' ' << 2 * x << '\n';
    }
    return 0;
}
