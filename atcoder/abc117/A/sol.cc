#include <bits/stdc++.h>

using namespace std;

int main()
{
    ios::sync_with_stdio(false);
    cin.tie(0);
    long double t, x;
    cin >> t >> x;
    cout << fixed << setprecision(12) << t/x << endl;
    return 0;
}
