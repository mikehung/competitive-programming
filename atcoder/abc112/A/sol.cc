#include <bits/stdc++.h>

using namespace std;

int main()
{
    ios::sync_with_stdio(false);
    cin.tie(0);
    int n;
    cin >> n;
    if (n == 1) cout << "Hello World\n";
    else {
        int a, b;
        cin >> a >> b;
        cout << (a+b) << endl;
    }
    return 0;
}
